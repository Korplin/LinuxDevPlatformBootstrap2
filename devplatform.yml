---
# =============================================================================
# devplatform.yml
# Linux Developer Platform Bootstrap — Stage 2
#
# Repository : https://github.com/Korplin/LinuxDevPlatformBootstrap2
# Target OS  : Debian 13 "trixie" amd64
# Run with   : ansible-playbook /tmp/devplatform.yml
#              (invoked automatically by devplatformbootstrap.sh)
#
# All tasks use ansible.builtin.* fully-qualified module names.
# The playbook is idempotent — every task is safe to run multiple times.
# =============================================================================

- name: Linux Developer Platform Bootstrap
  hosts: localhost
  connection: local
  become: true
  gather_facts: true

  vars:
    # ── Real (non-root) user detection ──────────────────────────────────────
    # When the bootstrap script runs via sudo, SUDO_USER is set to the
    # original caller's username. real_home is NOT hardcoded here; it is
    # resolved authoritatively from /etc/passwd in pre_tasks below.
    real_user: "{{ ansible_env.SUDO_USER | default(ansible_user_id) }}"

    # ── Pinned versions — bump here to update ───────────────────────────────
    nvm_version:     "v0.40.1"   # https://github.com/nvm-sh/nvm/releases
    kubectl_version: "v1.32"     # https://kubernetes.io/releases/

    # ── Cursor IDE download URL ──────────────────────────────────────────────
    # Cursor metadata API — returns JSON containing debUrl, version, etc.
    # The actual .deb download URL is extracted from the response in Section 11.
    cursor_api_url: "https://cursor.com/api/download?platform=linux-x64&releaseTrack=stable"

  # ============================================================================
  # PRE-TASKS: sanity checks + variable resolution before the main task list
  # ============================================================================
  pre_tasks:

    - name: "[PRE] Assert target is Debian 13 trixie"
      ansible.builtin.assert:
        that:
          - ansible_distribution == "Debian"
          - ansible_distribution_release == "trixie"
        fail_msg: >
          This playbook requires Debian 13 (trixie).
          Detected: {{ ansible_distribution }} {{ ansible_distribution_release }}.
        success_msg: "Confirmed: Debian 13 (trixie)"
      # Reason: Hard-fail immediately if the OS is wrong rather than silently
      # applying trixie-specific APT sources to an incompatible system.

    - name: "[PRE] Resolve real_home from the passwd database"
      ansible.builtin.command:
        cmd: "getent passwd {{ real_user }}"
      register: _passwd_entry
      changed_when: false
      failed_when: _passwd_entry.rc != 0
      # Reason: getent reads /etc/passwd authoritatively and handles any home
      # directory location — /home/x, /root, /data/users/x, etc. Hardcoding
      # "/home/{{ real_user }}" breaks for root and any custom home path.

    - name: "[PRE] Set real_home fact from passwd entry"
      ansible.builtin.set_fact:
        real_home: "{{ _passwd_entry.stdout.split(':')[5] }}"
      # Reason: The 6th colon-delimited field (index 5) of /etc/passwd is the
      # home directory — this is authoritative and platform-independent.

    - name: "[PRE] Display configuration summary"
      ansible.builtin.debug:
        msg:
          - "real_user : {{ real_user }}"
          - "real_home : {{ real_home }}"

  # ============================================================================
  handlers:
    - name: Restart ssh
      ansible.builtin.systemd:
        name: ssh
        state: restarted

  # ============================================================================
  tasks:

    # ==========================================================================
    # SECTION 1 — APT SOURCES
    # ==========================================================================

    - name: "[APT] Write /etc/apt/sources.list"
      ansible.builtin.copy:
        dest: /etc/apt/sources.list
        owner: root
        group: root
        mode: "0644"
        content: |
          # Debian 13 (trixie) — managed by Ansible (devplatform.yml)
          deb http://deb.debian.org/debian trixie main contrib non-free non-free-firmware
          deb-src http://deb.debian.org/debian trixie main contrib non-free non-free-firmware

          deb http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware
          deb-src http://security.debian.org/debian-security trixie-security main contrib non-free non-free-firmware

          deb http://deb.debian.org/debian trixie-updates main contrib non-free non-free-firmware
          deb-src http://deb.debian.org/debian trixie-updates main contrib non-free non-free-firmware
      # Reason: Since Debian 12, non-free-firmware is a separate component.
      # Both non-free AND non-free-firmware must be listed to get proprietary
      # GPU drivers (non-free) AND hardware firmware blobs like WiFi and CPU
      # microcode (non-free-firmware). contrib contains packages that depend
      # on non-free software.

    - name: "[APT] Create /etc/apt/keyrings directory"
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        owner: root
        group: root
        mode: "0755"
      # Reason: /etc/apt/keyrings is the Debian 12+ standard location for
      # per-repository GPG keys used with the signed-by= apt option.

    - name: "[APT] Refresh package cache after sources.list update"
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 0
      # Reason: cache_valid_time: 0 forces a full refresh immediately after
      # writing sources.list so subsequent tasks see the new components.

    # ==========================================================================
    # SECTION 2 — VM AND GPU DETECTION
    # ==========================================================================

    - name: "[DETECT] Install dmidecode and pciutils"
      ansible.builtin.apt:
        name:
          - dmidecode   # reads SMBIOS/DMI data for VM detection
          - pciutils    # provides lspci for GPU detection
        state: present

    - name: "[DETECT] Query system product name"
      ansible.builtin.shell:
        cmd: dmidecode -s system-product-name 2>/dev/null || echo "Unknown"
      register: _system_product
      changed_when: false
      failed_when: false

    - name: "[DETECT] Query system manufacturer"
      ansible.builtin.shell:
        cmd: dmidecode -s system-manufacturer 2>/dev/null || echo "Unknown"
      register: _system_manufacturer
      changed_when: false
      failed_when: false

    - name: "[DETECT] Query VGA adapter via lspci"
      ansible.builtin.shell:
        cmd: lspci | grep -i vga 2>/dev/null || echo ""
      register: _gpu_info
      changed_when: false
      failed_when: false

    - name: "[DETECT] Derive platform and GPU boolean facts"
      ansible.builtin.set_fact:
        is_virtualbox: >-
          {{ 'VIRTUALBOX' in (_system_product.stdout | upper) or
             'VIRTUALBOX' in (_system_manufacturer.stdout | upper) }}
        is_kvm: >-
          {{ 'QEMU' in (_system_product.stdout | upper) or
             'QEMU' in (_system_manufacturer.stdout | upper) or
             'KVM'  in (_system_product.stdout | upper) or
             'KVM'  in (_system_manufacturer.stdout | upper) }}
        has_nvidia: >-
          {{ 'NVIDIA' in (_gpu_info.stdout | upper) }}
        has_amd: >-
          {{ 'AMD'     in (_gpu_info.stdout | upper) or
             'RADEON'  in (_gpu_info.stdout | upper) or
             'ADVANCED MICRO' in (_gpu_info.stdout | upper) }}
        has_intel_gpu: >-
          {{ 'INTEL' in (_gpu_info.stdout | upper) }}

    - name: "[DETECT] Print detection summary"
      ansible.builtin.debug:
        msg:
          - "Product      : {{ _system_product.stdout | trim }}"
          - "Manufacturer : {{ _system_manufacturer.stdout | trim }}"
          - "GPU line     : {{ _gpu_info.stdout | trim }}"
          - "VirtualBox   : {{ is_virtualbox }}"
          - "KVM/QEMU     : {{ is_kvm }}"
          - "NVIDIA GPU   : {{ has_nvidia }}"
          - "AMD GPU      : {{ has_amd }}"
          - "Intel GPU    : {{ has_intel_gpu }}"

    # ==========================================================================
    # SECTION 3 — GPU AND VIRTUALISATION DRIVERS
    # ==========================================================================

    - name: "[GPU] Install VirtualBox guest utilities"
      ansible.builtin.apt:
        name:
          - virtualbox-guest-utils   # shared folders, clipboard, drag-and-drop
          - virtualbox-guest-x11     # display integration and auto-resize
        state: present
      when: is_virtualbox
      # Reason: VirtualBox guest additions enable seamless mouse, shared
      # clipboard, dynamic screen resolution, and accelerated display.
      # Physical GPU driver tasks are skipped when running inside VirtualBox.

    - name: "[GPU] Install KVM/QEMU guest utilities"
      ansible.builtin.apt:
        name:
          - spice-vdagent            # clipboard and auto-resize in SPICE sessions
          - qemu-guest-agent         # host↔guest communication channel
          - xserver-xorg-video-qxl   # QXL display driver for QEMU virtual GPU
        state: present
      when: is_kvm and not is_virtualbox

    - name: "[GPU] Install linux kernel headers (required for NVIDIA DKMS)"
      ansible.builtin.apt:
        name:
          - linux-headers-amd64   # metapackage — always tracks the running kernel
        state: present
      when: has_nvidia and not is_virtualbox and not is_kvm
      # Reason: The proprietary NVIDIA driver uses DKMS to compile a kernel
      # module against the running kernel. Without the matching headers, DKMS
      # compilation fails silently — the system boots but NVIDIA is not loaded.
      # linux-headers-amd64 is a metapackage that always depends on the current
      # kernel's headers, so it remains correct after kernel upgrades.

    - name: "[GPU] Install NVIDIA proprietary driver"
      ansible.builtin.apt:
        name:
          - nvidia-driver    # proprietary driver for GTX 500+ series
          - nvidia-settings  # GUI control panel for NVIDIA settings
        state: present
      when: has_nvidia and not is_virtualbox and not is_kvm
      register: _nvidia_install
      ignore_errors: true
      # Reason: nvidia-driver lives in non-free. ignore_errors: true prevents
      # a hard failure if a DKMS build is unavailable for the running kernel;
      # the nouveau blacklist task below only fires on a successful install.

    - name: "[GPU] Blacklist nouveau (open-source NVIDIA driver)"
      ansible.builtin.copy:
        dest: /etc/modprobe.d/blacklist-nouveau.conf
        owner: root
        group: root
        mode: "0644"
        content: |
          # Managed by devplatform.yml — do not edit manually.
          blacklist nouveau
          options nouveau modeset=0
      when:
        - has_nvidia
        - not is_virtualbox
        - not is_kvm
        - _nvidia_install is defined
        - _nvidia_install is succeeded
      # Reason: The proprietary NVIDIA driver and the open-source nouveau
      # driver cannot coexist. This modprobe rule prevents the kernel from
      # loading nouveau at boot, which is required for nvidia-driver to init.

    - name: "[GPU] Install AMD GPU driver packages"
      ansible.builtin.apt:
        name:
          - firmware-amd-graphics        # GPU firmware blobs (required for amdgpu)
          - libgl1-mesa-dri              # OpenGL via Mesa (radeonsi driver)
          - xserver-xorg-video-amdgpu    # Xorg DDX driver for AMD
        state: present
      when: has_amd and not is_virtualbox and not is_kvm

    - name: "[GPU] Install Intel GPU driver packages"
      ansible.builtin.apt:
        name:
          - intel-media-va-driver   # VA-API hardware video acceleration (Gen8+)
          - libgl1-mesa-dri         # OpenGL via Mesa (iris / i965 driver)
        state: present
      when: has_intel_gpu and not is_virtualbox and not is_kvm

    # ==========================================================================
    # SECTION 4 — WIRELESS AND HARDWARE FIRMWARE
    # ==========================================================================

    - name: "[FIRMWARE] Install wireless and general hardware firmware"
      ansible.builtin.apt:
        name:
          - firmware-linux           # generic Linux firmware bundle
          - firmware-linux-nonfree   # additional non-free firmware
          - firmware-iwlwifi         # Intel WiFi (AX200, AX210, 8260, 8265…)
          - firmware-realtek         # Realtek NICs and WiFi adapters
          - firmware-atheros         # Qualcomm/Atheros WiFi
          - firmware-brcm80211       # Broadcom WiFi (common in ThinkPads)
        state: present
      # Reason: A developer workstation may be on any hardware. Shipping all
      # common WiFi firmware ensures the system is usable on bare metal without
      # a separate post-install step. Unused firmware packages are harmless.

    # ==========================================================================
    # SECTION 5 — KDE PLASMA DESKTOP
    # ==========================================================================

    - name: "[KDE] Install KDE Plasma, SDDM, and core KDE applications"
      ansible.builtin.apt:
        name:
          - kde-full                          # complete KDE Plasma metapackage
          - sddm                              # Simple Desktop Display Manager
          - plasma-nm                         # NetworkManager applet for KDE
          - konsole                           # KDE terminal emulator
          - fonts-powerline                   # required for agnoster zsh theme
          - fonts-firacode                    # developer font with ligatures
        state: present
        install_recommends: true

    - name: "[KDE] Register sddm as the default display manager via debconf"
      ansible.builtin.debconf:
        name: sddm
        question: shared/default-x-display-manager
        value: sddm
        vtype: select
      # Reason: If multiple DMs are installed as indirect dependencies (e.g.,
      # gdm3 pulled in via a GNOME library), this debconf selection ensures
      # sddm wins and is used at next boot.

    - name: "[KDE] Create /etc/sddm.conf.d/ directory"
      ansible.builtin.file:
        path: /etc/sddm.conf.d
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: "[KDE] Write sddm configuration (Breeze theme, no auto-login)"
      ansible.builtin.copy:
        dest: /etc/sddm.conf.d/kde_settings.conf
        owner: root
        group: root
        mode: "0644"
        content: |
          # Managed by devplatform.yml
          [Theme]
          Current=breeze

          [Autologin]
          Relogin=false

    - name: "[KDE] Enable sddm service"
      ansible.builtin.systemd:
        name: sddm
        enabled: true
        daemon_reload: true
      # Reason: We enable (not start) sddm here because the display server
      # cannot start inside an Ansible session. It starts after reboot.

    # ==========================================================================
    # SECTION 6 — GIT AND GIT TOOLING
    # ==========================================================================

    - name: "[GIT] Download GitHub CLI GPG key"
      ansible.builtin.get_url:
        url: https://cli.github.com/packages/githubcli-archive-keyring.gpg
        dest: /etc/apt/keyrings/githubcli-archive-keyring.gpg
        owner: root
        group: root
        mode: "0644"
        force: false
      # Reason: force: false — the key is only downloaded if absent. Subsequent
      # runs skip the network request, making the task faster and idempotent.

    - name: "[GIT] Add GitHub CLI apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64
          signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg]
          https://cli.github.com/packages stable main
        state: present
        filename: github-cli
        update_cache: true

    - name: "[GIT] Install git and git tooling"
      ansible.builtin.apt:
        name:
          - git          # core VCS
          - git-lfs      # Large File Storage extension
          - tig          # ncurses text-mode repository browser
          - gitk         # Tcl/Tk GUI history viewer
          - gh           # official GitHub CLI
          - meld         # visual diff and merge tool
        state: present
      # Reason: git-flow is intentionally excluded. This platform uses
      # trunk-based development. git-flow is covered as historical context
      # in Module 1 but is not installed — tools shape habits.

    - name: "[GIT] Initialise git-lfs system-wide"
      ansible.builtin.command:
        cmd: git lfs install --system
      changed_when: false

    - name: "[GIT] Set system-wide git config: core.autocrlf = input"
      ansible.builtin.command:
        cmd: git config --system core.autocrlf input
      changed_when: false
      # Reason: 'input' converts CRLF → LF on commit but never adds CRLF to
      # checked-out files. Correct for Linux developers working with Windows
      # contributors.

    - name: "[GIT] Set system-wide git config: init.defaultBranch = main"
      ansible.builtin.command:
        cmd: git config --system init.defaultBranch main
      changed_when: false

    - name: "[GIT] Set system-wide git config: pull.rebase = true"
      ansible.builtin.command:
        cmd: git config --system pull.rebase true
      changed_when: false
      # Reason: trunk-based development produces a linear commit history.
      # pull.rebase = true keeps that history linear when pulling — it rebases
      # local commits on top of the fetched remote state instead of creating
      # a merge commit. This is the correct default for the branching strategy
      # taught in Module 1.

    - name: "[GIT] Set system-wide git config: rebase.autoStash = true"
      ansible.builtin.command:
        cmd: git config --system rebase.autoStash true
      changed_when: false
      # Reason: autoStash automatically stashes uncommitted changes before a
      # rebase and restores them after. This prevents the common beginner error
      # "cannot rebase: you have unstaged changes" when running git pull with
      # rebase enabled.

    # ==========================================================================
    # SECTION 7 — KVM VIRTUALISATION STACK
    # ==========================================================================

    - name: "[KVM] Install KVM virtualisation packages"
      ansible.builtin.apt:
        name:
          - qemu-kvm                 # KVM hypervisor and QEMU machine emulator
          - libvirt-daemon-system    # libvirtd system daemon
          - libvirt-clients          # virsh CLI and client libraries
          - virt-manager             # GTK GUI for managing VMs
          - virtinst                 # virt-install CLI for VM provisioning
          - virt-viewer              # lightweight SPICE/VNC VM console viewer
          - bridge-utils             # brctl for network bridging
          - ovmf                     # UEFI firmware for QEMU VMs
        state: present
      # Reason: ovmf (TianoCore UEFI) is required to install modern OSes
      # (Windows 11, Fedora with Secure Boot) inside KVM guests.

    - name: "[KVM] Add {{ real_user }} to libvirt group"
      ansible.builtin.user:
        name: "{{ real_user }}"
        groups: libvirt
        append: true
      # Reason: Without libvirt membership, the user gets "permission denied"
      # connecting to the libvirt socket. append: true is critical — omitting
      # it would remove the user from all other groups.

    - name: "[KVM] Add {{ real_user }} to kvm group"
      ansible.builtin.user:
        name: "{{ real_user }}"
        groups: kvm
        append: true
      # Reason: The kvm group grants r/w access to /dev/kvm. Without it,
      # hardware acceleration is unavailable and VMs run in pure software
      # emulation, which is extremely slow.

    - name: "[KVM] Enable and start libvirtd service"
      ansible.builtin.systemd:
        name: libvirtd
        enabled: true
        state: started

    # ==========================================================================
    # SECTION 8 — FLATPAK WITH KDE DISCOVER
    # ==========================================================================

    - name: "[FLATPAK] Install Flatpak and KDE Discover Flatpak backend"
      ansible.builtin.apt:
        name:
          - flatpak
          - plasma-discover-backend-flatpak   # Flatpak integration in Discover
        state: present

    - name: "[FLATPAK] Add Flathub remote"
      ansible.builtin.command:
        cmd: flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo
      changed_when: false
      # Reason: --if-not-exists is Flatpak's own idempotency guard.

    # ==========================================================================
    # SECTION 9 — BRAVE BROWSER
    # ==========================================================================

    - name: "[BRAVE] Download Brave Browser GPG key"
      ansible.builtin.get_url:
        url: https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
        dest: /usr/share/keyrings/brave-browser-archive-keyring.gpg
        owner: root
        group: root
        mode: "0644"
        force: false

    - name: "[BRAVE] Add Brave Browser apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg]
          https://brave-browser-apt-release.s3.brave.com/ stable main
        state: present
        filename: brave-browser
        update_cache: true

    - name: "[BRAVE] Install Brave Browser"
      ansible.builtin.apt:
        name: brave-browser
        state: present

    # ==========================================================================
    # SECTION 10 — VISUAL STUDIO CODE
    # ==========================================================================

    - name: "[VSCODE] Download Microsoft GPG key"
      ansible.builtin.get_url:
        url: https://packages.microsoft.com/keys/microsoft.asc
        dest: /tmp/microsoft.asc
        mode: "0644"
        force: true

    - name: "[VSCODE] Dearmor Microsoft GPG key into /etc/apt/keyrings/"
      ansible.builtin.shell:
        cmd: >
          gpg --dearmor < /tmp/microsoft.asc
          > /etc/apt/keyrings/packages.microsoft.gpg
        creates: /etc/apt/keyrings/packages.microsoft.gpg
      # Reason: apt's signed-by= requires a binary .gpg key, not ASCII .asc.
      # creates: skips the dearmor if the binary key already exists.

    - name: "[VSCODE] Add Visual Studio Code apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64
          signed-by=/etc/apt/keyrings/packages.microsoft.gpg]
          https://packages.microsoft.com/repos/code stable main
        state: present
        filename: vscode
        update_cache: true

    - name: "[VSCODE] Install Visual Studio Code"
      ansible.builtin.apt:
        name: code
        state: present

    # ==========================================================================
    # SECTION 11 — CURSOR IDE (.deb package)
    # ==========================================================================
    # Cursor does not publish an apt repository. Their API endpoint returns a
    # JSON object whose 'debUrl' field contains the versioned .deb download URL.
    # Example response:
    #   { "debUrl": "https://downloads.cursor.com/production/<sha>/...amd64.deb",
    #     "version": "3.1.17", "commitSha": "...", ... }
    # We resolve the URL at playbook runtime so we always get the latest release
    # without hardcoding a version string that goes stale on the next release.

    - name: "[CURSOR] Check if Cursor is already installed"
      ansible.builtin.command:
        cmd: dpkg-query -W -f='${Status}' cursor
      register: _cursor_dpkg
      changed_when: false
      failed_when: false
      # Reason: Exit code 1 means not installed. We use this as the idempotency
      # guard for all subsequent Cursor tasks. To upgrade Cursor:
      #   sudo apt remove cursor && ansible-playbook /tmp/devplatform.yml

    - name: "[CURSOR] Resolve latest .deb URL from Cursor API"
      ansible.builtin.uri:
        url: "{{ cursor_api_url }}"
        method: GET
        return_content: true
        status_code: 200
        headers:
          Accept: "application/json"
      register: _cursor_api
      when: _cursor_dpkg.rc != 0 or 'install ok installed' not in _cursor_dpkg.stdout
      # Reason: The API returns JSON containing debUrl, version, commitSha, etc.
      # ansible.builtin.uri parses JSON automatically when Content-Type is
      # application/json, making the field available as _cursor_api.json.debUrl.

    - name: "[CURSOR] Download Cursor .deb package"
      ansible.builtin.get_url:
        url: "{{ _cursor_api.json.debUrl }}"
        dest: /tmp/cursor-latest.deb
        mode: "0644"
        force: true
        timeout: 180
      when: _cursor_dpkg.rc != 0 or 'install ok installed' not in _cursor_dpkg.stdout
      # Reason: Using the resolved CDN URL (not the API endpoint) as the download
      # target avoids any redirect chains and ensures we get exactly the version
      # the API advertised.

    - name: "[CURSOR] Install Cursor .deb package"
      ansible.builtin.apt:
        deb: /tmp/cursor-latest.deb
        state: present
      when: _cursor_dpkg.rc != 0 or 'install ok installed' not in _cursor_dpkg.stdout
      # Reason: ansible.builtin.apt with deb: installs via dpkg, registers the
      # package in the apt database, and resolves any declared dependencies.
      # Using .deb instead of AppImage means:
      #   - No libfuse2 dependency
      #   - No --no-sandbox workaround needed (sandbox works correctly)
      #   - Proper system-wide installation under /usr/
      #   - Registered in dpkg — 'apt remove cursor' works cleanly

    - name: "[CURSOR] Remove downloaded .deb package"
      ansible.builtin.file:
        path: /tmp/cursor-latest.deb
        state: absent

    # ==========================================================================
    # SECTION 12 — NODE.JS VIA NVM
    # ==========================================================================

    - name: "[NVM] Check if nvm is already installed"
      ansible.builtin.stat:
        path: "{{ real_home }}/.nvm/nvm.sh"
      register: _nvm_stat

    - name: "[NVM] Install nvm {{ nvm_version }} for {{ real_user }}"
      ansible.builtin.shell:
        cmd: >
          curl -o-
          https://raw.githubusercontent.com/nvm-sh/nvm/{{ nvm_version }}/install.sh
          | bash
        executable: /bin/bash
      become: true
      become_user: "{{ real_user }}"
      when: not _nvm_stat.stat.exists
      environment:
        HOME: "{{ real_home }}"
        NVM_DIR: "{{ real_home }}/.nvm"
      # Reason: nvm is a shell function, not a binary — it must be installed
      # per-user. become_user ensures ~/.nvm lands in the real user's home.
      # RUNZSH and CHSH are not set here because we handle .zshrc integration
      # explicitly in the Shell section, which is more reliable than letting
      # the nvm installer append to files we also manage.

    - name: "[NVM] Install latest Node.js LTS via nvm"
      ansible.builtin.shell: |
        export NVM_DIR="{{ real_home }}/.nvm"
        [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
        nvm install --lts
        nvm alias default node
      args:
        executable: /bin/bash
      become: true
      become_user: "{{ real_user }}"
      environment:
        HOME: "{{ real_home }}"
        NVM_DIR: "{{ real_home }}/.nvm"
      register: _nvm_node_install
      changed_when: "'already installed' not in _nvm_node_install.stdout"

    - name: "[NVM] Install global npm packages"
      ansible.builtin.shell: |
        export NVM_DIR="{{ real_home }}/.nvm"
        [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
        npm install -g pnpm yarn typescript ts-node nodemon http-server
      args:
        executable: /bin/bash
      become: true
      become_user: "{{ real_user }}"
      environment:
        HOME: "{{ real_home }}"
        NVM_DIR: "{{ real_home }}/.nvm"
      changed_when: false
      # Reason: These global packages install into the nvm-managed Node prefix,
      # not the system npm, so they stay isolated and are updated when Node is
      # updated via nvm. changed_when: false suppresses always-changed output.

    # ==========================================================================
    # SECTION 13 — PYTHON AND TOOLING
    # ==========================================================================

    - name: "[PYTHON] Install Python system packages"
      ansible.builtin.apt:
        name:
          - python3        # Python 3 interpreter
          - python3-pip    # pip package installer
          - python3-venv   # venv module for virtual environments
          - python3-dev    # C header files for building Python extensions
          - pipx           # isolated application installer (PEP 668-safe)
        state: present
      # Reason: python3-dev is required to compile C extension modules
      # (cryptography, psycopg2, etc.). pipx installs CLI tools in isolated
      # virtualenvs, respecting PEP 668 which prevents pip from modifying
      # the system Python installation.

    - name: "[PYTHON] Ensure ~/.local/bin is on PATH in .profile"
      ansible.builtin.lineinfile:
        path: "{{ real_home }}/.profile"
        line: 'export PATH="$HOME/.local/bin:$PATH"'
        state: present
        create: true
        mode: "0644"
      become: true
      become_user: "{{ real_user }}"

    - name: "[PYTHON] Install ansible-lint via pipx"
      ansible.builtin.command:
        cmd: pipx install ansible-lint
        creates: "{{ real_home }}/.local/pipx/venvs/ansible-lint"
      become: true
      become_user: "{{ real_user }}"
      environment:
        HOME: "{{ real_home }}"
        PIPX_HOME: "{{ real_home }}/.local/pipx"
        PIPX_BIN_DIR: "{{ real_home }}/.local/bin"

    - name: "[PYTHON] Install pre-commit via pipx"
      ansible.builtin.command:
        cmd: pipx install pre-commit
        creates: "{{ real_home }}/.local/pipx/venvs/pre-commit"
      become: true
      become_user: "{{ real_user }}"
      environment:
        HOME: "{{ real_home }}"
        PIPX_HOME: "{{ real_home }}/.local/pipx"
        PIPX_BIN_DIR: "{{ real_home }}/.local/bin"

    - name: "[PYTHON] Install cookiecutter via pipx"
      ansible.builtin.command:
        cmd: pipx install cookiecutter
        creates: "{{ real_home }}/.local/pipx/venvs/cookiecutter"
      become: true
      become_user: "{{ real_user }}"
      environment:
        HOME: "{{ real_home }}"
        PIPX_HOME: "{{ real_home }}/.local/pipx"
        PIPX_BIN_DIR: "{{ real_home }}/.local/bin"

    - name: "[PYTHON] Check if uv is already installed"
      ansible.builtin.stat:
        path: "{{ real_home }}/.local/bin/uv"
      register: _uv_stat

    - name: "[PYTHON] Install uv (modern Python package manager)"
      ansible.builtin.shell:
        cmd: curl -LsSf https://astral.sh/uv/install.sh | sh
        executable: /bin/bash
      become: true
      become_user: "{{ real_user }}"
      when: not _uv_stat.stat.exists
      environment:
        HOME: "{{ real_home }}"
      # Reason: uv is not yet in Debian trixie. The official installer places
      # a static binary at ~/.local/bin/uv. The stat check makes this idempotent.

    # ==========================================================================
    # SECTION 14 — CORE DEVOPS UTILITIES (apt packages)
    # ==========================================================================

    - name: "[DEVOPS] Install core DevOps utility packages"
      ansible.builtin.apt:
        name:
          # ── File and data utilities ──────────────────────────────────────
          - curl
          - wget
          - jq             # JSON processor (the original and authoritative one)
          - unzip
          - zip
          - tar
          - rsync
          - p7zip-full     # 7z archive support
          - unrar          # RAR archive extraction
          # ── System monitoring ────────────────────────────────────────────
          - htop           # interactive process viewer
          - btop           # modern resource monitor with graphs
          - ncdu           # ncurses disk usage analyser
          - tree           # recursive directory listing
          # ── Modern CLI replacements ──────────────────────────────────────
          - fzf            # fuzzy finder — transforms terminal workflow
          - bat            # cat replacement with syntax highlighting
          - eza            # ls/tree replacement (trixie has it in repos)
          - ripgrep        # grep replacement — used by VS Code search internally
          - fd-find        # find replacement (saner syntax, faster)
          # ── Terminal multiplexers ────────────────────────────────────────
          - tmux           # terminal multiplexer (screen is deliberately excluded)
          # ── Network utilities ────────────────────────────────────────────
          - net-tools      # ifconfig, netstat — legacy but useful for teaching
          - nmap           # network scanner
          - traceroute
          - dnsutils       # dig, nslookup, host
          - whois
          # ── SSH ──────────────────────────────────────────────────────────
          - openssh-client
          - openssh-server
          # ── Build tools ──────────────────────────────────────────────────
          - build-essential    # gcc, g++, make, libc-dev
          - cmake
          - pkg-config
          # ── Secret management (GitOps, Module 6) ────────────────────────
          - age                # modern file encryption (used with SOPS)
          # ── Ansible (ensure present after optional bootstrap install) ────
          - ansible
        state: present
      # Reason: fzf, bat, eza, ripgrep, fd-find are the modern replacements
      # for fuzzy matching, cat, ls, grep, and find. These are tools students
      # will actually use daily and see referenced in every modern tutorial.
      # screen is excluded — tmux is the modern choice and teaching both
      # creates confusion with no benefit.
      # Note: yq is NOT installed from apt. The Debian 'yq' package is a
      # Python wrapper with different syntax from the mikefarah Go binary
      # that all documentation refers to. See Section 15 (yq).

    - name: "[DEVOPS] Add {{ real_user }} to docker group"
      ansible.builtin.user:
        name: "{{ real_user }}"
        groups: docker
        append: true
      # Reason: Without docker group membership, every docker command requires
      # sudo. Note: docker group membership is equivalent to passwordless sudo
      # for container operations — this is a documented Docker security tradeoff.
      # append: true is mandatory to avoid removing other group memberships.

    - name: "[DEVOPS] Create fd → fdfind symlink"
      ansible.builtin.file:
        src: /usr/bin/fdfind
        dest: /usr/local/bin/fd
        state: link
      # Reason: Debian packages fd-find as 'fdfind' to avoid a conflict with
      # a legacy fdclone package. The entire world calls it 'fd'. This symlink
      # makes the standard name work without requiring shell aliases.

    # ==========================================================================
    # SECTION 15 — yq (Go binary — mikefarah/yq)
    # ==========================================================================

    - name: "[YQ] Check if yq (Go) is already installed"
      ansible.builtin.stat:
        path: /usr/local/bin/yq
      register: _yq_stat

    - name: "[YQ] Install yq Go binary (mikefarah/yq)"
      ansible.builtin.get_url:
        url: https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
        dest: /usr/local/bin/yq
        owner: root
        group: root
        mode: "0755"
        force: false
      when: not _yq_stat.stat.exists
      # Reason: The Debian 'yq' package is a Python wrapper around jq that uses
      # jq syntax — NOT yq syntax. Every tutorial, Stack Overflow answer, and
      # documentation page for 'yq' refers to the Go-based mikefarah/yq, which
      # uses a YAML-native query language. Installing the Debian package and
      # then following any external yq resource will produce failures or wrong
      # output. This installs the correct binary.
      # To upgrade: delete /usr/local/bin/yq and re-run the playbook.

    # ==========================================================================
    # SECTION 16 — DOCKER CE (official Docker repository)
    # ==========================================================================

    - name: "[DOCKER] Download Docker GPG key and install to keyrings"
      ansible.builtin.shell:
        cmd: >
          curl -fsSL https://download.docker.com/linux/debian/gpg
          | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
        creates: /etc/apt/keyrings/docker.gpg
      # Reason: Piping directly to gpg --dearmor handles both ASCII-armored
      # and binary key formats gracefully. creates: makes this idempotent.

    - name: "[DOCKER] Set correct permissions on Docker GPG key"
      ansible.builtin.file:
        path: /etc/apt/keyrings/docker.gpg
        owner: root
        group: root
        mode: "0644"

    - name: "[DOCKER] Add Docker CE apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64
          signed-by=/etc/apt/keyrings/docker.gpg]
          https://download.docker.com/linux/debian trixie stable
        state: present
        filename: docker-ce
        update_cache: true

    - name: "[DOCKER] Install Docker CE"
      ansible.builtin.apt:
        name:
          - docker-ce              # Docker Engine
          - docker-ce-cli          # Docker CLI
          - containerd.io          # Container runtime
          - docker-buildx-plugin   # BuildKit extended build capabilities
          - docker-compose-plugin  # 'docker compose' v2 (replaces docker-compose v1)
        state: present
      # Reason: docker.io (the Debian-packaged Docker) is multiple major versions
      # behind Docker CE from Docker's official repository. docker-compose (v1,
      # standalone Python binary) was deprecated in 2023. docker-compose-plugin
      # is the current v2 implementation, invoked as 'docker compose' (no hyphen).

    - name: "[DOCKER] Enable and start Docker service"
      ansible.builtin.systemd:
        name: docker
        enabled: true
        state: started

    # ==========================================================================
    # SECTION 17 — kubectl
    # ==========================================================================

    - name: "[KUBECTL] Download Kubernetes apt signing key"
      ansible.builtin.get_url:
        url: "https://pkgs.k8s.io/core:/stable:/{{ kubectl_version }}/deb/Release.key"
        dest: /tmp/kubernetes-release.key
        mode: "0644"
        force: true
      # Reason: force: true ensures a rotated key is always picked up.

    - name: "[KUBECTL] Dearmor Kubernetes GPG key"
      ansible.builtin.shell:
        cmd: >
          gpg --dearmor < /tmp/kubernetes-release.key
          > /etc/apt/keyrings/kubernetes-apt-keyring.gpg
        creates: /etc/apt/keyrings/kubernetes-apt-keyring.gpg

    - name: "[KUBECTL] Add Kubernetes apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg]
          https://pkgs.k8s.io/core:/stable:/{{ kubectl_version }}/deb/ /
        state: present
        filename: kubernetes
        update_cache: true
      # Reason: Kubernetes publishes per-minor-version apt repositories.
      # Updating kubectl_version in vars and re-running switches version streams.

    - name: "[KUBECTL] Install kubectl"
      ansible.builtin.apt:
        name: kubectl
        state: present

    # ==========================================================================
    # SECTION 18 — Helm
    # ==========================================================================

    - name: "[HELM] Download Helm signing key"
      ansible.builtin.get_url:
        url: https://baltocdn.com/helm/signing.asc
        dest: /tmp/helm-signing.asc
        mode: "0644"
        force: true

    - name: "[HELM] Dearmor Helm GPG key"
      ansible.builtin.shell:
        cmd: >
          gpg --dearmor < /tmp/helm-signing.asc
          > /usr/share/keyrings/helm.gpg
        creates: /usr/share/keyrings/helm.gpg

    - name: "[HELM] Add Helm stable apt repository"
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64 signed-by=/usr/share/keyrings/helm.gpg]
          https://baltocdn.com/helm/stable/debian/ all main
        state: present
        filename: helm-stable-debian
        update_cache: true

    - name: "[HELM] Install helm"
      ansible.builtin.apt:
        name: helm
        state: present

    # ==========================================================================
    # SECTION 19 — k9s (terminal UI for Kubernetes)
    # ==========================================================================

    - name: "[K9S] Check if k9s is already installed"
      ansible.builtin.stat:
        path: /usr/local/bin/k9s
      register: _k9s_stat

    - name: "[K9S] Download k9s tarball"
      ansible.builtin.get_url:
        url: https://github.com/derailed/k9s/releases/latest/download/k9s_Linux_amd64.tar.gz
        dest: /tmp/k9s.tar.gz
        mode: "0644"
      when: not _k9s_stat.stat.exists
      # Reason: k9s is not in Debian repositories. GitHub releases is the
      # official distribution channel.

    - name: "[K9S] Extract k9s binary to /usr/local/bin"
      ansible.builtin.unarchive:
        src: /tmp/k9s.tar.gz
        dest: /usr/local/bin/
        remote_src: true
        extra_opts:
          - k9s     # extract only the k9s binary, skip LICENSE and README
      when: not _k9s_stat.stat.exists
      # Reason: k9s is the essential terminal UI for Kubernetes. Once students
      # reach Module 5 (K3s) and Module 6 (ArgoCD), they will use k9s daily.
      # Installing it now means it is ready without an additional setup step.

    - name: "[K9S] Ensure k9s binary is executable"
      ansible.builtin.file:
        path: /usr/local/bin/k9s
        mode: "0755"
      when: not _k9s_stat.stat.exists

    # ==========================================================================
    # SECTION 20 — cosign (container image signing)
    # ==========================================================================

    - name: "[COSIGN] Check if cosign is already installed"
      ansible.builtin.stat:
        path: /usr/local/bin/cosign
      register: _cosign_stat

    - name: "[COSIGN] Install cosign binary"
      ansible.builtin.get_url:
        url: https://github.com/sigstore/cosign/releases/latest/download/cosign-linux-amd64
        dest: /usr/local/bin/cosign
        owner: root
        group: root
        mode: "0755"
        force: false
      when: not _cosign_stat.stat.exists
      # Reason: cosign is required in Module 3 (container image pipeline) for
      # signing and verifying images using sigstore. It is also required for
      # supply-chain security verification in Module 6 (GitOps). Installing now
      # avoids a mid-module setup interruption.

    # ==========================================================================
    # SECTION 21 — SHELL AND TERMINAL SETUP
    # ==========================================================================

    - name: "[SHELL] Install zsh"
      ansible.builtin.apt:
        name: zsh
        state: present

    - name: "[SHELL] Set zsh as default login shell for {{ real_user }}"
      ansible.builtin.user:
        name: "{{ real_user }}"
        shell: /bin/zsh
      # Reason: ansible.builtin.user writes directly to /etc/passwd, which is
      # idempotent and does not prompt for a password (unlike chsh).

    - name: "[SHELL] Check if oh-my-zsh is already installed"
      ansible.builtin.stat:
        path: "{{ real_home }}/.oh-my-zsh"
      register: _ohmyzsh_stat

    - name: "[SHELL] Install oh-my-zsh (unattended)"
      ansible.builtin.shell:
        cmd: >
          sh -c "$(curl -fsSL
          https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"
          "" --unattended
        executable: /bin/bash
      become: true
      become_user: "{{ real_user }}"
      when: not _ohmyzsh_stat.stat.exists
      environment:
        HOME: "{{ real_home }}"
        RUNZSH: "no"    # prevents installer from exec-ing zsh — would hang Ansible
        CHSH: "no"      # we set the shell via ansible.builtin.user above

    - name: "[SHELL] Clone zsh-autosuggestions plugin"
      ansible.builtin.git:
        repo: https://github.com/zsh-users/zsh-autosuggestions
        dest: "{{ real_home }}/.oh-my-zsh/custom/plugins/zsh-autosuggestions"
        version: master
        update: false
      become: true
      become_user: "{{ real_user }}"
      # Reason: update: false — git clone is a no-op if the directory exists.
      # Change to update: true to pull plugin updates on every playbook run.

    - name: "[SHELL] Clone zsh-syntax-highlighting plugin"
      ansible.builtin.git:
        repo: https://github.com/zsh-users/zsh-syntax-highlighting
        dest: "{{ real_home }}/.oh-my-zsh/custom/plugins/zsh-syntax-highlighting"
        version: master
        update: false
      become: true
      become_user: "{{ real_user }}"

    - name: "[SHELL] Set ZSH_THEME to agnoster in .zshrc"
      ansible.builtin.lineinfile:
        path: "{{ real_home }}/.zshrc"
        regexp: '^ZSH_THEME='
        line: 'ZSH_THEME="agnoster"'
        state: present
      become: true
      become_user: "{{ real_user }}"

    - name: "[SHELL] Enable plugins in .zshrc"
      ansible.builtin.lineinfile:
        path: "{{ real_home }}/.zshrc"
        regexp: '^plugins='
        line: "plugins=(git fzf zsh-autosuggestions zsh-syntax-highlighting)"
        state: present
      become: true
      become_user: "{{ real_user }}"
      # Reason: The fzf oh-my-zsh plugin binds Ctrl-R (history search),
      # Ctrl-T (file picker), and Alt-C (cd into directory) to fzf-powered
      # fuzzy interfaces — a transformative improvement to terminal workflow.

    - name: "[SHELL] Add nvm init block to .zshrc"
      ansible.builtin.blockinfile:
        path: "{{ real_home }}/.zshrc"
        marker: "# {mark} ANSIBLE MANAGED BLOCK — nvm"
        block: |
          export NVM_DIR="$HOME/.nvm"
          [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
          [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"
        state: present
        create: true
      become: true
      become_user: "{{ real_user }}"
      # Reason: nvm's install script appends init lines to ~/.bashrc but not
      # ~/.zshrc. Without this block, nvm and node are unavailable in zsh.

    - name: "[SHELL] Add ~/.local/bin to PATH in .zshrc"
      ansible.builtin.blockinfile:
        path: "{{ real_home }}/.zshrc"
        marker: "# {mark} ANSIBLE MANAGED BLOCK — local bin PATH"
        block: |
          export PATH="$HOME/.local/bin:$PATH"
        state: present
        create: true
      become: true
      become_user: "{{ real_user }}"
      # Reason: pipx, uv, pre-commit, cookiecutter, and ansible-lint all
      # install to ~/.local/bin. This block makes them available in every
      # interactive zsh session without requiring the user to edit .zshrc.

    - name: "[SHELL] Add useful shell aliases for modern CLI tools"
      ansible.builtin.blockinfile:
        path: "{{ real_home }}/.zshrc"
        marker: "# {mark} ANSIBLE MANAGED BLOCK — modern CLI aliases"
        block: |
          # Modern CLI tool aliases — installed by devplatform.yml
          alias cat='bat --paging=never'     # syntax-highlighted cat
          alias ls='eza --icons'             # ls with icons and git status
          alias ll='eza -la --icons --git'   # long listing with git status
          alias tree='eza --tree --icons'    # directory tree
          alias grep='rg'                    # ripgrep as default grep
          alias find='fd'                    # fd as default find
        state: present
        create: true
      become: true
      become_user: "{{ real_user }}"
      # Reason: These aliases replace legacy tools with their modern equivalents
      # while keeping familiar command names. Students learn the new tools
      # immediately without needing to remember new command names. They can
      # override any alias by running the full path (e.g., /usr/bin/grep).

    # ==========================================================================
    # SECTION 22 — FINAL SYSTEM UPGRADE AND CLEANUP
    # ==========================================================================

    - name: "[FINAL] Full system upgrade (dist-upgrade)"
      ansible.builtin.apt:
        upgrade: full
        update_cache: true
      # Reason: Pulls in security updates published after the netinst ISO was
      # created. Ensures the machine is not left with known vulnerabilities.

    - name: "[FINAL] Remove orphaned packages"
      ansible.builtin.apt:
        autoremove: true
        purge: true

    - name: "[FINAL] Clean apt package cache"
      ansible.builtin.apt:
        autoclean: true
      # Reason: Removes downloaded .deb files from /var/cache/apt/archives
      # that are no longer available in the repository. Keeps disk usage clean.

    - name: "[FINAL] Remove temporary files created during bootstrap"
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - /tmp/microsoft.asc
        - /tmp/kubernetes-release.key
        - /tmp/helm-signing.asc
        - /tmp/k9s.tar.gz
        - /tmp/cursor-latest.deb

    - name: "[FINAL] Print post-install instructions"
      ansible.builtin.debug:
        msg:
          - "═══════════════════════════════════════════════════════════════"
          - " ✔  Developer Platform Bootstrap — COMPLETE"
          - "═══════════════════════════════════════════════════════════════"
          - ""
          - "  REQUIRED STEPS:"
          - ""
          - "  1. Reboot:"
          - "       sudo reboot"
          - "     Required for: GPU drivers, kernel modules, sddm,"
          - "     nouveau blacklist, and all group memberships."
          - ""
          - "  2. After reboot, KDE Plasma starts via sddm."
          - "     Log in with your normal user credentials."
          - ""
          - "  3. Open Konsole. Your shell is now zsh + oh-my-zsh."
          - "     In Konsole Settings → Fonts, select a Powerline-capable"
          - "     font (e.g., FiraCode Nerd Font or 'DejaVu Sans Mono')"
          - "     for the agnoster theme arrows to render correctly."
          - ""
          - "  4. Set your git identity (per-user, not system-wide):"
          - "       git config --global user.name  'Your Name'"
          - "       git config --global user.email 'you@example.com'"
          - ""
          - "  5. Authenticate the GitHub CLI:"
          - "       gh auth login"
          - ""
          - "  6. Group memberships active after reboot:"
          - "       docker, libvirt, kvm"
          - ""
          - "  7. Tools available in new zsh session:"
          - "       node  npm  pnpm  yarn  tsc  ts-node  nodemon"
          - "       ansible-lint  pre-commit  cookiecutter  uv"
          - "       kubectl  helm  k9s  cosign  yq  jq  fzf"
          - "       bat  eza  ripgrep (rg)  fd  age"
          - "═══════════════════════════════════════════════════════════════"
